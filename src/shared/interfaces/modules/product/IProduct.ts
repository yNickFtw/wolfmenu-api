export interface IProduct {
  id: string;
  name: string;
  description: string;
  price: number;
  productImage: string;
  productImageFilename: string;
  userId: string;
  categoryId: string;
  unitId: string
}