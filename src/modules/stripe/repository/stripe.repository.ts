import { IStripe } from "../../../shared/interfaces/modules/stripe/IStripe";
import { IStripeRepository } from "../../../shared/interfaces/modules/stripe/repository/IStripeRepository";
import { Stripe } from "../entity/stripe.schema";

export default class StripeRepository implements IStripeRepository {
    
}